请基于当前 `JustinQP/codex-job` 项目，完成 `v1.0.0：稳定版收口、验收矩阵与文档重构`。

## 背景

当前项目已经完成到 v0.9 收口阶段：

```text
v0.8.0：App Server 会话模式主线集成
v0.8.1：完整 final 保存、bridge_thread_id 校验
v0.8.2：主线 App Server smoke 脚本、mobile 操作体验增强
v0.8.3：一键启动、smoke 失败清理、AppThread reopen 恢复能力
v0.9.0：App Turn 异步执行与手机端轮询
v0.9.1：异步 App Turn 并发保护、本地取消、执行超时
v0.9.x：stale recovery、event summary、筛选、archived 清理、README 收口
```

当前链路：

```text
主线 Runner/codex exec 任务链路：
mobile / API
  ↓
backend/main.py
  ↓
Task API
  ↓
runner
  ↓
codex exec

App Server 会话链路：
mobile / API
  ↓
backend/main.py
  ↓
App Thread / App Turn API
  ↓
backend/services/app_thread_service.py
  ↓
backend/services/app_turn_executor.py
  ↓
backend/services/app_server_bridge_client.py
  ↓ HTTP
poc/app_server/app_server_bridge.py
  ↓ stdio
codex app-server
```

v0.9 已经具备：

```text
异步 App Turn
轮询
本地取消
并发保护
stale turn 恢复
event summary
AppThread/AppTurn 筛选
archived 清理
smoke 验收脚本
mobile 操作入口
```

本次 v1.0.0 不继续加大功能，而是做稳定版收口。

---

# 一、v1.0.0 总目标

完成一个可作为当前阶段稳定版的本地工具版本。

目标：

```text
1. 统一项目版本到 1.0.0。
2. 重构 README，去掉版本流水账堆叠，形成稳定使用文档。
3. 新增/整理 docs 文档，明确 API、状态机、App Server sidecar 架构。
4. 固化 smoke 验收矩阵。
5. 保证主线 Runner/codex exec 链路不被破坏。
6. 保证 App Server 同步/异步/recover/conflict smoke 路径清晰。
7. mobile 只做小修，不大改 UI。
8. 测试继续全绿。
```

---

# 二、重要约束

1. 不做新大功能。
2. 不做 SSE。
3. 不做审批 UI。
4. 不做 diff UI。
5. 不引入 Redis/Celery/Docker。
6. 不引入 Vue/React/前端工程化。
7. 不引入新依赖。
8. 不修改 Runner/codex exec 主链路语义。
9. 不把 `poc/app_server/app_server_bridge.py` 合并进 `backend/main.py`。
10. 不做真正强杀 Codex App Server turn。
11. 不做跨进程恢复后台线程。
12. 不做多用户权限。
13. 不做公网部署。
14. 不自动 commit。
15. 不 push。
16. 修改范围保持小，以收口和文档为主。

---

# 三、任务 1：版本号统一为 1.0.0

修改文件：

```text
backend/main.py
README.md
```

## backend/main.py

当前 FastAPI version 仍可能是：

```python
version="0.8.1"
```

请改为：

```python
version="1.0.0"
```

## README.md

在 README 顶部明确：

```text
当前版本：v1.0.0
定位：Codex Remote Runner + App Server Sidecar 本地控制台
```

要求：

1. 不要继续把 README 主体写成 v0.8/v0.9 版本流水账。
2. 历史版本可以放到“版本摘要”小节，但不要占据主要篇幅。
3. 明确 App Server 仍是 experimental sidecar，不替代 Runner/codex exec 主链路。

---

# 四、任务 2：README 重构为稳定文档

修改文件：

```text
README.md
```

将 README 重构为以下结构。

## 推荐结构

```text
# Codex Remote Runner

## 1. 项目定位

## 2. 核心能力

## 3. 快速开始

## 4. 主线 Runner/codex exec 任务链路

## 5. App Server sidecar 会话链路

## 6. 手机控制台

## 7. Smoke 验收矩阵

## 8. API 概览

## 9. 状态机

## 10. 常见问题

## 11. 当前限制

## 12. 版本摘要
```

## 重点内容要求

### 1. 项目定位

说明：

```text
本项目是一个本地/局域网使用的 Codex Remote Runner 控制台。
主线能力是通过 Runner 执行 codex exec 任务。
App Server 能力是 sidecar POC，用于探索 codex app-server 会话模式。
```

必须明确：

```text
App Server Bridge 仍作为 sidecar 独立运行。
App Server 不替换 Runner/codex exec 主链路。
当前不建议公网暴露。
```

### 2. 快速开始

保留一键启动：

```powershell
.\scripts\start_app_server_stack.ps1 -ApiToken dev-token -BridgeToken dev-token
```

带 smoke：

```powershell
.\scripts\start_app_server_stack.ps1 -ApiToken dev-token -BridgeToken dev-token -RunSmoke
```

保留手动启动：

```powershell
$env:APP_SERVER_BRIDGE_TOKEN="dev-token"
python .\poc\app_server\app_server_bridge.py --host 127.0.0.1 --port 8766
```

```powershell
$env:API_TOKEN="dev-token"
$env:APP_SERVER_BRIDGE_URL="http://127.0.0.1:8766"
$env:APP_SERVER_BRIDGE_TOKEN="dev-token"
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

访问：

```text
http://127.0.0.1:8000/mobile
```

### 3. Smoke 验收矩阵

写清楚以下命令。

#### 后端基础自检

```powershell
python -m compileall backend runner scripts poc/app_server
pytest -q
```

#### App Server 同步 smoke

```powershell
$env:API_TOKEN="dev-token"
python .\scripts\smoke_app_server_flow.py --base-url http://127.0.0.1:8000 --project-path F:\JustinKing\codex-job
```

#### App Server 异步 smoke

```powershell
$env:API_TOKEN="dev-token"
python .\scripts\smoke_app_server_flow.py --base-url http://127.0.0.1:8000 --project-path F:\JustinKing\codex-job --async-turn
```

#### App Server 并发保护 smoke

```powershell
$env:API_TOKEN="dev-token"
python .\scripts\smoke_app_server_flow.py --base-url http://127.0.0.1:8000 --project-path F:\JustinKing\codex-job --async-turn --check-async-conflict
```

#### stale turn 恢复 smoke

```powershell
$env:API_TOKEN="dev-token"
python .\scripts\smoke_app_server_flow.py --base-url http://127.0.0.1:8000 --recover-stale
```

### 4. 当前限制

必须包含：

```text
- 当前主要面向本地/可信局域网使用。
- API Token 不是完整多用户权限系统。
- App Server 异步执行是单进程线程池。
- 服务重启后不会继续执行 RUNNING/PENDING AppTurn，会将其恢复为 FAILED。
- 取消是本地状态取消，不保证中断正在执行的 Codex App Server turn。
- APP_TURN_EXECUTION_TIMEOUT_SECONDS 是 Bridge HTTP 调用超时，不是强杀 Codex 子进程。
- 不支持 SSE。
- 不支持审批 UI。
- 不支持 diff UI。
- 不替换 Runner/codex exec 主链路。
```

---

# 五、任务 3：新增 docs 文档

新增目录：

```text
docs/
```

新增文件：

```text
docs/api-overview.md
docs/app-server-session.md
docs/state-machines.md
docs/smoke-checklist.md
```

如果 `docs/` 已存在，则只新增或整理这些文件，不要删除已有重要文档。

---

## 3.1 docs/api-overview.md

内容目标：

说明当前 API 分组。

至少包含：

```text
1. Health
2. Projects
3. Tasks
4. Runners
5. App Server Bridge
6. AppThreads
7. AppTurns
```

必须列出 App Server 相关 API：

```text
GET  /app-server-bridge/health

GET    /app-threads
POST   /app-threads
GET    /app-threads/{id}
PATCH  /app-threads/{id}
DELETE /app-threads/{id}
POST   /app-threads/{id}/reopen
POST   /app-threads/cleanup

POST /app-threads/{id}/turns
POST /app-threads/{id}/turns/async
GET  /app-threads/{id}/turns
GET  /app-threads/{id}/final
GET  /app-threads/{id}/events

GET  /app-turns/{id}
POST /app-turns/{id}/cancel
POST /app-turns/recover-stale
```

说明哪些需要 `X-API-Token`。

---

## 3.2 docs/app-server-session.md

内容目标：

说明 App Server sidecar 架构和使用方式。

必须包含架构图：

```text
mobile / API
  ↓
backend/main.py
  ↓
App Thread / App Turn API
  ↓
app_thread_service.py
  ↓
app_turn_executor.py
  ↓
app_server_bridge_client.py
  ↓ HTTP
poc/app_server/app_server_bridge.py
  ↓ stdio
codex app-server
```

说明：

```text
1. Bridge sidecar 独立运行。
2. 主后端只通过 HTTP 调用 Bridge。
3. 不直接在 backend/main.py 中启动 codex app-server。
4. 不替代 Runner/codex exec 主链路。
5. Bridge 重启后旧 bridge_thread_id 可能失效，可以使用 reopen。
6. 后端重启后 PENDING/RUNNING turn 会被 recover-stale 标记为 FAILED。
```

---

## 3.3 docs/state-machines.md

内容目标：

说明状态机。

必须包含：

### AppThread 状态

```text
CREATED
ACTIVE
ERROR
CLOSED
```

说明状态含义和典型流转：

```text
CREATED -> ACTIVE
ACTIVE -> ERROR
ACTIVE -> CLOSED
ERROR -> ACTIVE by reopen
CLOSED -> ACTIVE by reopen
```

### AppTurn 状态

```text
PENDING
RUNNING
SUCCESS
FAILED
CANCELLED
```

说明典型流转：

```text
PENDING -> RUNNING -> SUCCESS
PENDING -> RUNNING -> FAILED
PENDING -> CANCELLED
RUNNING -> CANCELLED
PENDING/RUNNING -> FAILED by recover-stale
```

必须说明：

```text
CANCELLED 是本地状态取消，不保证中断 Codex App Server。
recover-stale 是失败恢复，不会重放用户消息。
```

---

## 3.4 docs/smoke-checklist.md

内容目标：

形成验收清单。

至少包含：

```text
1. compileall
2. pytest
3. backend health
4. bridge health
5. App Server 同步 smoke
6. App Server 异步 smoke
7. 并发保护 smoke
8. recover-stale smoke
9. mobile 手工验收
```

mobile 手工验收至少包含：

```text
- 保存 API Token
- 检查 Bridge
- 创建 AppThread
- 同步发送 AppTurn
- 异步发送 AppTurn
- 刷新当前 Turn
- 取消当前 Turn
- 查看 final
- 查看 events
- reopen AppThread
- recover stale AppTurn
- 状态筛选
- 清理 CLOSED/ERROR
```

---

# 六、任务 4：mobile 小修，不大改 UI

修改文件：

```text
backend/mobile.py
```

只做小修，不重写页面。

## 4.1 App Server 区块顶部增加能力说明

在 App Server 会话区块顶部增加一段说明：

```text
App Server 会话为 sidecar POC：支持同步/异步 turn、轮询、本地取消、reopen、recover-stale、筛选与 archived 清理；不替代 Runner/codex exec 主链路。
```

## 4.2 cleanup 操作增加 confirm

清理 CLOSED / ERROR 前增加确认：

```text
确认将 CLOSED AppThread 标记为 archived？
确认将 ERROR AppThread 标记为 archived？
```

取消则不调用 API。

## 4.3 recover stale 增加 confirm

调用 recover stale 前确认：

```text
确认将所有 PENDING/RUNNING AppTurn 标记为 FAILED？
```

取消则不调用 API。

## 4.4 archived 显示提示

当勾选“显示 archived”后，列表里 title 以 `[archived]` 开头的 AppThread 增加灰色提示：

```text
archived
```

可以用简单文本，不需要复杂 CSS。

## 4.5 安全要求

所有动态写入 `innerHTML` 的字段继续使用 `escapeHtml()`。

---

# 七、任务 5：固定验收命令脚本或文档

优先文档化，不强制新增脚本。

如果新增脚本，允许新增：

```text
scripts/check_v1_smoke.ps1
```

但不是必须。

如果新增，请做到：

```powershell
python -m compileall backend runner scripts poc/app_server
pytest -q
```

以及打印 smoke 命令提示。

不要在该脚本中默认启动真实 Codex 或真实 Bridge，避免测试不可控。

---

# 八、任务 6：测试补充

修改或新增测试：

```text
tests/test_ui.py
tests/test_app_threads_api.py
tests/test_app_thread_service.py
tests/test_smoke_app_server_flow.py
```

允许新增：

```text
tests/test_docs.py
```

## 6.1 README/docs 静态测试

新增 `tests/test_docs.py`，做轻量静态检查。

覆盖：

```text
README.md 存在 v1.0.0
README.md 包含 /mobile
README.md 包含 smoke_app_server_flow.py
README.md 包含 App Server sidecar
README.md 包含 Runner/codex exec
docs/api-overview.md 存在
docs/app-server-session.md 存在
docs/state-machines.md 存在
docs/smoke-checklist.md 存在
```

不要做过度脆弱的全文匹配，只检查关键短语。

## 6.2 mobile 测试

在 `tests/test_ui.py` 中补充检查：

```text
App Server 会话为 sidecar POC
confirm(`确认将 CLOSED AppThread 标记为 archived？`)
confirm(`确认将 ERROR AppThread 标记为 archived？`)
confirm(`确认将所有 PENDING/RUNNING AppTurn 标记为 FAILED？`)
archived
```

并继续确认关键动态字段使用 `escapeHtml()`。

## 6.3 API 测试

如果当前已有 API 测试，不需要重复大量新增。只补必要测试：

```text
FastAPI version 是 1.0.0
```

可通过读取 `app.version` 或 `/docs` 不方便则直接 import `backend.main.app`。

---

# 九、自检命令

完成后必须运行：

```powershell
python -m compileall backend runner scripts poc/app_server
pytest -q
```

如果测试失败：

1. 不要伪造通过。
2. 先修复属于本次范围的问题。
3. 如果失败不是本次范围，明确说明原因。

---

# 十、允许修改文件

允许修改：

```text
backend/main.py
backend/mobile.py
README.md
tests/test_ui.py
tests/test_app_threads_api.py
tests/test_app_thread_service.py
tests/test_smoke_app_server_flow.py
```

允许新增：

```text
docs/api-overview.md
docs/app-server-session.md
docs/state-machines.md
docs/smoke-checklist.md
tests/test_docs.py
scripts/check_v1_smoke.ps1
```

原则上不要修改：

```text
runner/
poc/app_server/
backend/models.py
backend/db.py
backend/services/app_thread_service.py
backend/services/app_turn_executor.py
backend/services/app_server_bridge_client.py
```

除非发现 v1.0 收口必须修的小问题，并说明原因。

---

# 十一、完成后输出

请按以下格式输出：

```text
本次完成：
-

修改文件：
-

新增文件：
-

验证结果：
- python -m compileall backend runner scripts poc/app_server
- pytest -q

风险与未完成项：
-

后续建议：
-
```

不要自动 commit。
不要 push。
